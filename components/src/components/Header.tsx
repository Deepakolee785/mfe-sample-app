import React from "react";

const Header = () => {
  return (
    <header
      style={{
        height: "50px",
        background: "purple",
        color: "#fff",
        fontWeight: 700,
        fontSize: "1.5rem",
        padding: "1rem 0 0 1rem",
      }}
    >
      Header
    </header>
  );
};

export default Header;
